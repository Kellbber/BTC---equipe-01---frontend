import { StringLiteral } from "typescript";

export type Institution = {
    name: string;
    phone: string;
    cep: string;
    adressNumber: string;
    street: String;
    district: string;
    city: string;
    state: string;
    complement: string;
}